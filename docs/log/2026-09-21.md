# 2026-09-21 の週

## やったこと

- CIの設計書を記載
- CIにLintの追加した。
- CIに型チェックを追加した。

## 決めたことと理由

- **CIの設計書をci.mdに記載する**
  正直、まだ理解できていない点が多いから

- **pushを外してPRだけにした**
  理由：現状、CIはPR作成時とpush時に2回走るようになっていたがそれは無駄ではないか？mainへのpushでも回す案は採らなかった。必須チェックをstrictに認めPRの結果がmainの結果と同じになると考えたため。

- **lintをCIに足した**
  理由：使用していないコードやimportが入っていないことを確認するために行う。

- **型チェックをCIに足した**

## 学んだこと

- ADRについて：Architecture Decision Record意思決定の記録のことを指す。『何を決めたか』ではなく『なぜ、そう決めたのか』を決めた時点で1件1ファイルに残す書き方を学んだ。（これ、どこにも記載していないよね）
- mermaidの書き方について
- package.jsonのscriptの記載方法
- 複数実行したいときは&&を用いて記載すること。

## 宿題

- Lintの学習
- [ ]そもそもLintが何か？を自分の言葉で説明する。

- [ ]pushを外す前と後のCIの時間計測
  料金に差が出るのか？料金計算を確認する。

- [ ]型チェックを入れる利点

- [ ]CIの構成について考える

## やることを整理する

- CIに型チェックを導入する。

## 本日のエラー

- 型チェックを追加する際に起こしたエラー
  ①npm run typeをCIに追加した
  以下のエラーが起きた。
  npm error Missing script: "type"
  npm error
  npm error To see a list of scripts, run:
  npm error npm run
  npm error A complete log of this run can be found in: /home/runner/.npm/\_logs/2026-09-23T08_48_41_480Z-debug-0.log

package.jsonにtypeを追加していないために起きたエラーかな。
これを後ほど確かめる。

解決方法
package.jsonのscriptにtypecheckを追加して
"typecheck": "tsc --noEmit"を追加したことでエラーは解除された。

- 2つ目
  Error: src/app/layout.tsx(20,50): error TS2304: Cannot find name 'LayoutProps'.
  Error: Process completed with exit code 2.

  LayoutPropsがglobalでimportが不要なパッケージって話。
  これが.nextがないからかな。

  CIは.nextがない状態から始まるからねー
  それを回避するためにbuildしたりしないといけないかな。

## 整理

package.jsonのscriptsにあるものをnpm run 〇〇にdevなどを入れる実行されることであっているか？
"tsc --noEmit"これはどこにあるのか？

script名にtypecheckを採用した理由：
package.jsonには"type":"module"という別の設定項目があり、これと分けたいと考えたため。
